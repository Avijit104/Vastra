export const gender = {
  male: "male",
  female: "female",
  other: "other",
};
export const availableGender = Object.values(gender);

export const roles = {
  customer: "customer",
  seller: "seller",
  admin: "admin",
};
export const availableRole = Object.values(roles);

export const addressType = {
  home: "home",
  work: "work",
  business: "business",
};
export const availableAddressType = Object.values(addressType);

export const category = {};
export const availableCategory = Object.values(category);

export const keywords = {};
export const availableKeywords = Object.values(keywords);

export const orderStatus = {
  placed: "placed",
  packed: "packed",
  shipped: "shipped",
  return: "return",
  delivered: "delivered",
};
export const availableOrderStatus = Object.values(orderStatus);

export const priority = {
  high: "high",
  medium: "medium",
  low: "low",
};
export const availablePriority = Object.values(priority);
